# 开发日志

## 维护规则

- 本文件用于记录每次代码提交后的开发记录。
- 日志按工作分支分组维护，每个分支只保留一个分组标题。
- 每次提交代码后，必须在对应分支分组下新增一条提交记录。
- 同一分支下的新提交放在最上方，旧提交按时间倒序保留。
- 每条记录至少包含：
  - 提交时间
  - 提交哈希
  - 提交信息
  - 用户提出的需求
  - 本次完成的工作
  - 验证情况
- `用户提出的需求` 应优先保留用户原话，只做最小整理。
- 文档提交如果没有额外验证，需明确写明“文档提交，无额外代码验证”。

## codex/workflow-modal-ux

### 2026-04-26 11:33 · cad91c7 · feat(frontend): improve workflow preset modal

- 用户提出的需求：
  - “让git 忽略这些文件吧，无需追踪”
  - “好，继续吧”
  - “你怎么把左右排列变成上下排列了？”

- 本次完成的工作：
  - 将工作流管理弹窗拆分为独立的 `WorkflowPresetModal` 组件，减轻 `PlaceholderCard` 的体积和职责
  - 将工作流预设表单逻辑提炼到独立 helper，包括默认草稿、从预设生成草稿、副本草稿、脏状态判断、阶段预览与阶段重排
  - 将弹窗文案调整为“工作流预设”，补充说明文案，明确这里管理的是可复用模板而不是运行控制
  - 为预设列表增加“复制”能力，并在当前编辑的预设上显示“编辑中”状态
  - 为表单增加实时预览摘要、阶段上移/下移能力，以及关闭或切换草稿前的未保存确认
  - 修复工作流预设弹窗左右分栏布局，恢复双栏展示
  - 为本地 `.codex/`、`backend/workspace/` 以及指定计划文档补充 `.gitignore` 规则
  - 补充前端回归测试，覆盖预设复制、阶段重排、摘要更新、未保存离开确认和新文案

- 验证情况：
  - `pnpm exec vitest run src/components/__tests__/PlaceholderCard.workflow.test.tsx`
  - `pnpm check`

## main

### 2026-04-22 00:56 · 0b0f051 · feat: fix calendar overlap and auto-advance countdown workflows

- 用户提出的需求：
  - “你看看多周日历视图，长条任务和原点日程位置发生冲突了”
  - “我想更新机制：如果是倒计时的话则从专注阶段切换到休息阶段是自动切换的，不需要等待用户确认”

- 本次完成的工作：
  - 修复多周日历里点状日程与跨日长条任务的布局冲突
  - 为日历点状条目补充统一的高度、间距与可见数量布局常量
  - 让整周长条任务层根据该周点状条目占位高度整体下移，避免重叠
  - 调整前端回归测试，覆盖“同周同时存在点状日程与长条任务时长条必须避让”的场景
  - 更新专注工作流同步机制：倒计时 `focus` 阶段自然到点后自动推进到下一阶段
  - 保留正计时阶段、手动跳过阶段、运行时选任务等现有行为不变
  - 新增后端回归测试，覆盖“倒计时专注超时后自动切换到休息阶段”

- 验证情况：
  - `pnpm test -- src/components/__tests__/CalendarDayCell.test.tsx src/components/__tests__/MultiWeekCalendarGrid.test.tsx src/components/__tests__/MultiWeekCalendarModal.test.tsx`
  - `pnpm test -- src/lib/__tests__/workflowNotifications.test.ts src/components/__tests__/PlaceholderCard.workflow.test.tsx src/components/__tests__/WorkflowProgressBar.test.tsx src/components/__tests__/WorkflowNavProgress.test.tsx`
  - `pnpm check`
  - `uv run pytest tests/test_todo_routes.py -k workflow -v`
  - `uv run ruff check routes/todo_routes.py tests/test_todo_routes.py`

### 2026-04-21 19:17 · aa9b131 · test(backend): stabilize recurring appointment completion test

- 用户提出的需求：
  - “可以了，合并到主分支吧，删除这个分支吧”

- 本次完成的工作：
  - 在将 `codex/event-achievements-design` 本地快进合并到 `main` 后复跑前后端验证
  - 发现 `tests/test_todo_routes.py` 中一条重复日程完成测试使用固定过去时间，已随当前日期漂移而失败
  - 将该测试改为使用相对当前时间的未来结束时间，避免继续随日期变化失效
  - 为后续删除功能分支清理掉合并后 `main` 上的测试阻塞

- 验证情况：
  - `pnpm test -- --run src/components/__tests__/AchievementCard.test.tsx src/components/__tests__/AchievementModal.test.tsx src/components/__tests__/PlaceholderCard.workflow.test.tsx src/components/__tests__/RightPanel.test.tsx`
  - `pnpm check`
  - `uv run pytest tests/test_todo_routes.py -v`

## codex/complete-achievement-pool

### 2026-04-21 20:39 · 7335607 · feat: complete achievement pool

- 用户提出的需求：
  - “好，我希望你继续这个任务”
  - “但是要另起分支来做，不要再main分支上进行开发”
  - “我希望有些可以量化的成就可以设置一些进度条，比如说总专注xx小时这些成就下方可以显示总专注了多少个小时”
  - “已专注 25 小时 / 目标 50 小时 这种只显示前半部分就可以，后面目标其实就是成就名的详情处，不用重复说一次”
  - “太臃肿了，就和其他卡片一样，显示 成就 就行了”
  - “不对，我觉得这样也不是不行，默认显示 成就 ，但是可以保留鼠标悬浮上去显示摘要”

- 本次完成的工作：
  - 在 `codex/complete-achievement-pool` 分支补齐第一版成就池
  - 新增事件成就：`连续推进 3 天`、`守住关键节点`、`交付前夜`
  - 新增全局成就：`累计完成 100 项安排`、`连续使用 7 天`
  - 为可量化成就徽章增加进度条，专注类成就显示当前累计小时数，例如 `已专注 25 小时`
  - 将首页成就入口改为默认只显示 `成就`，鼠标悬浮或键盘聚焦时显示摘要，点击后进入完整弹窗
  - 补充后端成就摘要测试和前端成就组件测试

- 验证情况：
  - `uv run pytest`
  - `uv run ruff check routes/todo_routes.py tests/test_todo_routes.py`
  - `pnpm test -- --run src/components/__tests__/AchievementBadgeCard.test.tsx src/components/__tests__/AchievementCard.test.tsx src/components/__tests__/AchievementModal.test.tsx`
  - `pnpm check`

## codex/repeat-completion-events

### 2026-04-20 14:46 · 44fed46 · fix(todo): keep manually scheduled tasks in today summary

- 用户提出的需求：
  - “等等，关于第一个要求，是我说的不够清楚：只显示 period_type === 'daily' 且日期命中今天的任务，但也显示从右栏拖拽过来的任务”
  - “我想在安排管理右边的任务板块中仍然可以看到已完成的任务，但是无法拖到左边的今日任务中”
  - “编辑任务是不是还没有更新，我看到很多旧参数”

- 本次完成的工作：
  - 调整安排总览“今日任务”的最终展示规则
  - 不是所有“今天日期命中”的任务都会进入今日任务区
  - 仅显示两类任务：
    - `period_type === 'daily'` 且命中今天的任务
    - 用户手动从右侧拖拽到左侧今日区的任务
  - 为任务新增 `manually_scheduled_for_today` 标记
  - 右拖左 `move-to-today` 时设置该标记
  - 左拖右 `move-out-of-today` 时清除该标记
  - 前后端同步更新该字段的类型、接口返回和测试
- 验证情况：
  - `uv run pytest tests/test_todo_routes.py`
  - `pnpm test -- --run src/components/__tests__/TaskEditModal.test.tsx src/components/__tests__/AppointmentEditModal.test.tsx src/components/__tests__/PlaceholderCard.arrangements.test.tsx src/components/__tests__/PlaceholderCard.taskAssistant.test.tsx src/components/__tests__/EventCountdownCard.test.tsx`
  - `pnpm check`

### 2026-04-20 14:28 · 3118eab · feat(todo): move today tasks back to arrangements

- 用户提出的需求：
  - “然后是关于 安排管理处左右两侧的任务拖拽功能 ，现在是右边可以拖拽到左边，现在我想更新新功能，左侧的任务可以拖拽到右边，但
1. 如果今日任务的任务截止时间恰好是今天，就要提示用户。比如说如果是每日任务或者是每周任务（但今天是一周的最后一天），就要提示用户。”
  - “具体要看左右任务是什么类型”
  - “不希望改变周期”

- 本次完成的工作：
  - 新增左侧“今日任务”拖回右侧任务仓库的能力
  - 左拖右时不会修改任务周期，只会将任务移出今日
  - 如果任务当天处于当前周期截止边界，拖回右侧前会弹出确认提示
  - 为事件继承时间的任务补上移出今日时的时间覆盖处理，避免再次被事件时间拉回今天
  - 补充前后端测试覆盖左拖右行为
- 验证情况：
  - `uv run pytest tests/test_todo_routes.py`
  - `pnpm test -- --run src/components/__tests__/TaskEditModal.test.tsx src/components/__tests__/AppointmentEditModal.test.tsx src/components/__tests__/PlaceholderCard.arrangements.test.tsx src/components/__tests__/PlaceholderCard.taskAssistant.test.tsx src/components/__tests__/EventCountdownCard.test.tsx`
  - `pnpm check`

### 2026-04-20 11:26 · d56d79b · feat(todo): support recurring completion and event-bound arrangements

- 用户提出的需求：
  - “对于 任务 或者 日程 部分。它们现在应该还不支持重复完成，对吗？我希望可以实现重复完成，且添加时间条件，比如说：
1. 有些只可以在工作日完成
2. 有些一天只能完成一次，有些一周只能完成一次，有些在xx天内只能完成一次，总而言之有一个时间周期，单个时间周期内只能完成一次。”
  - “还有一个问题：我希望任务/日程是和事件绑定的，它的截止时间也是事件的截止时间。你看看关于 事件 的代码”
  - “你有没有修改 创建安排 这一弹窗，我发现事件参数处是填空的，我希望是选择框为默认选择当前事件。再看看其他参数需不需要修改”

- 本次完成的工作：
  - 为任务和日程实现重复完成能力
  - 支持完成周期、单周期最大完成次数、仅工作日可完成
  - 新增完成记录与周期完成状态计算
  - 任务和日程支持绑定事件，并可继承事件截止时间
  - 创建安排弹窗默认选择当前主事件
  - 清理任务创建/编辑中的旧循环参数
  - 右侧任务仓库筛选统一改为基于 `period_type`
  - 重复任务在当前周期完成后继续保留在右侧已完成区域中，但不可再拖入今日任务
  - 安排总览逐步重构为更符合新语义的展示逻辑
- 验证情况：
  - `uv run pytest tests/test_todo_routes.py`
  - `pnpm test -- --run src/components/__tests__/TaskEditModal.test.tsx src/components/__tests__/AppointmentEditModal.test.tsx src/components/__tests__/PlaceholderCard.arrangements.test.tsx src/components/__tests__/PlaceholderCard.taskAssistant.test.tsx src/components/__tests__/EventCountdownCard.test.tsx`
  - `pnpm check`

### 2026-04-20 · ce6b31b · docs: add repeat completion and event binding design

- 用户提出的需求：
  - “对于 任务 或者 日程 部分。它们现在应该还不支持重复完成，对吗？我希望可以实现重复完成，且添加时间条件，比如说：
1. 有些只可以在工作日完成
2. 有些一天只能完成一次，有些一周只能完成一次，有些在xx天内只能完成一次，总而言之有一个时间周期，单个时间周期内只能完成一次。”
  - “还有一个问题：我希望任务/日程是和事件绑定的，它的截止时间也是事件的截止时间。你看看关于 事件 的代码”

- 本次完成的工作：
  - 新增重复完成与事件绑定方案设计文档
  - 沉淀任务/日程重复完成、事件绑定、安排管理重构的设计依据
- 验证情况：
  - 文档提交，无额外代码验证

## codex/event-achievements-design

### 2026-04-21 19:17 · 4616857 · feat(frontend): animate achievement modal exit

- 用户提出的需求：
  - “好”

- 本次完成的工作：
  - 为成就弹窗增加 200ms 的退场动画，关闭时不再瞬间消失
  - 将成就弹窗改为“延迟卸载”模式，等待退场动画完成后再从 DOM 中移除
  - 为退场遮罩层和弹窗主体补充自定义 CSS keyframes，保留与现有入场动画兼容的视觉语言
  - 更新前端回归测试，覆盖 Esc 关闭后的延迟卸载、滚动锁定恢复时机与退场动画类

- 验证情况：
  - `pnpm test -- --run src/components/__tests__/AchievementModal.test.tsx`
  - `pnpm test -- --run src/components/__tests__/AchievementCard.test.tsx src/components/__tests__/AchievementModal.test.tsx src/components/__tests__/PlaceholderCard.workflow.test.tsx src/components/__tests__/RightPanel.test.tsx`
  - `pnpm check`

### 2026-04-21 19:07 · 1fb2c33 · feat(frontend): animate achievement modal

- 用户提出的需求：
  - “加动画吧”

- 本次完成的工作：
  - 为成就弹窗遮罩层补充淡入动画，使打开时的背景过渡更平滑
  - 为成就弹窗主体补充缩放进入动画，和现有任务/日程弹窗的动效语言保持一致
  - 增加前端回归测试，确保成就弹窗 overlay 与 surface 都保留入场动画类

- 验证情况：
  - `pnpm test -- --run src/components/__tests__/AchievementModal.test.tsx`
  - `pnpm test -- --run src/components/__tests__/AchievementCard.test.tsx src/components/__tests__/AchievementModal.test.tsx src/components/__tests__/PlaceholderCard.workflow.test.tsx src/components/__tests__/RightPanel.test.tsx`
  - `pnpm check`

### 2026-04-21 19:02 · f25ea25 · test(frontend): cover achievement modal scroll lock

- 用户提出的需求：
  - “好，继续补吧”

- 本次完成的工作：
  - 将成就弹窗的页面滚动锁定从仅处理 `body` 扩展为同时处理 `html` 与 `body`
  - 关闭成就弹窗时恢复打开前已有的滚动样式，避免覆盖页面原本状态
  - 补充成就弹窗回归测试，覆盖打开时锁定页面滚动与关闭后恢复原样

- 验证情况：
  - `pnpm test -- --run src/components/__tests__/AchievementModal.test.tsx`
  - `pnpm test -- --run src/components/__tests__/AchievementCard.test.tsx src/components/__tests__/AchievementModal.test.tsx src/components/__tests__/PlaceholderCard.workflow.test.tsx src/components/__tests__/RightPanel.test.tsx`
  - `pnpm check`

### 2026-04-21 18:57 · 63a5eaf · feat(frontend): improve achievement modal focus

- 用户提出的需求：
  - “什么焦点管理？”
  - “OK”

- 本次完成的工作：
  - 为成就卡片补充触发按钮引用，用于弹窗关闭后的焦点回收
  - 为 `AchievementModal` 增加打开后自动聚焦关闭按钮的行为
  - 支持按 `Esc` 关闭成就弹窗，并在关闭后把焦点还回成就卡片
  - 增加弹窗内 `Tab` / `Shift+Tab` 的焦点循环，避免焦点跳出到右栏其他区域
  - 补充前端回归测试，覆盖打开后聚焦与 `Esc` 关闭后的焦点恢复

- 验证情况：
  - `pnpm test -- --run src/components/__tests__/AchievementCard.test.tsx src/components/__tests__/AchievementModal.test.tsx src/components/__tests__/PlaceholderCard.workflow.test.tsx src/components/__tests__/RightPanel.test.tsx`
  - `pnpm check`

### 2026-04-21 09:22 · f60e041 · fix(frontend): portal achievement modal

- 用户提出的需求：
  - “点击成就卡片后，怎么不是跳出弹窗？只是展开而已，看不完全东西”

- 本次完成的工作：
  - 将 `AchievementModal` 改为通过 `createPortal` 挂载到 `document.body`
  - 为成就弹窗补充 `dialog` 语义，避免它继续表现成右栏内部展开层
  - 调整弹窗容器的视口高度与顶边留白，减少内容看不全的问题
  - 补充回归测试，确保弹窗不会再渲染在右栏容器内部

- 验证情况：
  - `pnpm test -- --run src/components/__tests__/AchievementCard.test.tsx`
  - `pnpm test -- --run src/components/__tests__/AchievementModal.test.tsx`
  - `pnpm test -- --run src/components/__tests__/AchievementCard.test.tsx src/components/__tests__/AchievementModal.test.tsx src/components/__tests__/PlaceholderCard.workflow.test.tsx src/components/__tests__/RightPanel.test.tsx`
  - `pnpm check`

### 2026-04-20 18:49 · 3722cae · feat(frontend): add event achievement surfaces

- 用户提出的需求：
  - “最好是每个事件都有自己独立的一套成就进度，切换事件就切换战绩，当然也有不依赖于事件的全局成就，比如说总共专注时长”
  - “全局成就总是出现，即使存在事件时也会显示全局成就”
  - “应该有两个ui，一个是卡片状态，另外一个是点开后的弹窗状态”
  - “给我展示的页面背景做成黑色（注意不是卡片状态和弹窗状态本身，而是展示用的背景）”
  - “可以，不错”
  - “好，继续吧”
  - “OK”

- 本次完成的工作：
  - 新增 `AchievementCard`、`AchievementModal`、`AchievementBadgeCard` 与共享类型
  - 将右侧面板原本静态的“成就”占位替换成真实的成就入口组件
  - 实现卡片状态与弹窗状态两层 UI
  - 实现弹窗中的事件切换，以及始终显示的全局成就区域
  - 补充前端单测覆盖卡片摘要、弹窗打开、事件切换和入口挂载

- 验证情况：
  - `pnpm test -- --run src/components/__tests__/AchievementCard.test.tsx src/components/__tests__/AchievementModal.test.tsx src/components/__tests__/PlaceholderCard.workflow.test.tsx src/components/__tests__/RightPanel.test.tsx`
  - `pnpm check`

### 2026-04-20 18:49 · d3f1446 · feat(todo): derive event achievements summary

- 用户提出的需求：
  - “C。对了，我希望成就也是跟着事件走的，你看看关于事件的代码再回复我吧”
  - “最好是每个事件都有自己独立的一套成就进度，切换事件就切换战绩，当然也有不依赖于事件的全局成就，比如说总共专注时长”
  - “全局成就总是出现，即使存在事件时也会显示全局成就”

- 本次完成的工作：
  - 新增 `/todo/achievements/summary` 汇总接口
  - 为成就响应补充事件成就与全局成就的结构化返回模型
  - 基于现有 `events`、`tasks`、`appointments`、`completion_records`、`focus_logs` 实现第一版派生计算
  - 事件成就按 `event_id` 过滤完成记录与专注时长
  - 全局成就始终返回，不依赖主事件是否存在
  - 顺手修复一个时间脆弱的后端测试，使其使用相对当前时间而非固定过去时间

- 验证情况：
  - `uv run pytest tests/test_todo_routes.py -k "achievement_summary_without_primary_event or achievement_summary_defaults_to_primary_event" -v`
  - `uv run pytest tests/test_todo_routes.py -k "filters_event_completions_and_focus" -v`
  - `uv run pytest tests/test_todo_routes.py -v`

### 2026-04-20 18:32 · 54d88b1 · docs: add event achievements design

- 用户提出的需求：
  - “看看成就板块相关的代码”
  - “好啊，我们继续讨论接下来怎么办吧”
  - “C的轻量版”
  - “有没有demo，文字还是太苍白了”
  - “C。对了，我希望成就也是跟着事件走的，你看看关于事件的代码再回复我吧”
  - “最好是每个事件都有自己独立的一套成就进度，切换事件就切换战绩，当然也有不依赖于事件的全局成就，比如说总共专注时长”
  - “全局成就总是出现，即使存在事件时也会显示全局成就”
  - “应该有两个ui，一个是卡片状态，另外一个是点开后的弹窗状态”

- 本次完成的工作：
  - 梳理成就入口、事件模型、任务 / 日程 / 专注与事件的现有关系
  - 通过浏览器讨论页迭代确认“事件成就 + 常驻全局成就”的产品方向
  - 确认成就板块存在两个独立 UI：卡片状态与弹窗状态
  - 新增事件成就设计文档，明确产品模型、成就池、接口形状、前端组件拆分、空状态和测试范围
  - 在独立分支 `codex/event-achievements-design` 上提交设计文档

- 验证情况：
  - 文档提交，无额外代码验证

## 备注

- 当前工作区可能存在未跟踪的本地文件，这些文件不应自动视为开发日志的一部分。
